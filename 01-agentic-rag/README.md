
## Setting up API keys
Create a `.env` file in your project folder and initialize it with the text:

```bash
OPENAI_API_KEY=sk-YOUR_KEY_HERE
```

Replace the texts on the right of equals for your personal keys for each requires service.